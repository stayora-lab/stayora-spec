# Payment Attempt Interaction

Where a canonical condition permits an attempt: show applicable condition and payer context where known; record attempt initiation; represent PROCESSING; then SUCCEEDED, FAILED or UNKNOWN according to authoritative outcome. Provider, account requirement and retry policy are not selected. Guest account is not required unless canonical.
