# Accept / Reject Interaction

Decision path: acting capacity → authority → Request current state → Property/Unit/date scope → relevant Inventory → applicable conditions → revalidation → commitment → outcome. Exact control/confirmation UI is not defined. Accept and Reject are consequential decisions with attributable provenance.
