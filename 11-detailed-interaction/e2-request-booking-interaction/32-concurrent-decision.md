# Concurrent Decision

Two authorized actors may view the same Request. The second commitment revalidates current Request, authority, scope and Inventory. It cannot overwrite the canonical result of the first action; it sees current truth and preserves both actor/provenance histories. No technical locking model is specified.
