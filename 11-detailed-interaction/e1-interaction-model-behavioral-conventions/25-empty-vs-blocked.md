# Empty vs Blocked

EMPTY means a legitimate scope has no objects. PARTIAL means the context exists but relationship/setup is incomplete. BLOCKED means the user wants an action but authority, policy or precondition prevents it. CONFLICT means truths cannot safely coexist. UNKNOWN means authoritative outcome is not known. Each requires different recovery and explanation categories; none is solved by a universal CTA.
