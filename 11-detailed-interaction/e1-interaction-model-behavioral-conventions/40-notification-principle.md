# Notification Principle

Notifications are delivery mechanisms, not canonical truth. A notification points to the current scoped projection; the current projection may have changed since delivery. Users must not commit consequential action from stale notification content without revalidation. Channels and notification design are not specified.
