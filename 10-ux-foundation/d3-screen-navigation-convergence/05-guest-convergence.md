# Guest Stay Access Convergence

**Primary scoped destination:** `GST-01 Stay Hub`.

`GST-02 Arrival & Access` is a scoped contextual access view reached from the Stay Hub; it may compose with the Hub later without merging Stay and credential semantics. `GST-03 Help & Operational Contact` is a contextual projection/action entry. `GST-04 Review Entry` is a conditional action entry after eligible completion, not a primary destination.

Guest primarily navigates a represented Stay, not a Booking workspace. Full account remains optional where canonical; scoped access does not grant operational or commercial authority.
