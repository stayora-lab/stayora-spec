# Destination / BQL Convergence

**Primary destination:** `BQL-01 Operations Today`.

`BQL-02 Stay Operations Collection` is a contextual collection from Today; `BQL-03 Incident & Exception` is contextual attention; `BQL-04 Access & Services Context` is a scoped service/access projection. BQL does not become Host, Booking, Inventory, Butler management or Admin. Destination/function scope governs every projection.
