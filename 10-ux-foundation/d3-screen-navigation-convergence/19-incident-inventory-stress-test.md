# Incident ↔ Inventory Stress Test

Incidents live in operational/contextual attention (`BUT-04`, `HST-10`, `BQL-03`, `ADM-04`). Inventory Intervention (`HST-09`) is a separate authorized action context. Evidence may hand off to an Inventory decision, but an Incident does not become a Maintenance Block automatically. Responsibility, consequence and policy remain separate.
